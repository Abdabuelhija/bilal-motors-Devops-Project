apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bilal-motors.fullname" . }}-test-connection"
  labels:
    {{- include "bilal-motors.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bilal-motors.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
