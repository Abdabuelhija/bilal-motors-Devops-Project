replicaCount: 1

image:
  repository: abedabu28/bilal-motors-frontend
  tag: "1.0"
  pullPolicy: IfNotPresent

containerPort: 3000

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: bilal-motors.local
      path: /
  tls: []   # مثال: - secretName: frontend-tls  hosts: [bilal-motors.example.com]

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}


config:
  VITE_API_URL: "http://api.bilal-motors.local"
